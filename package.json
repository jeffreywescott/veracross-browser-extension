{
  "name": "veracross-parent-digest",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "Browser extension: a private digest of the Veracross parent portal.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "zip": "rm -f dist/parent-digest.zip && mkdir -p dist && zip -r dist/parent-digest.zip manifest.json src icons -x '*.DS_Store'",
    "safari": "open 'safari/Parent Digest/Parent Digest.xcodeproj'",
    "icons": "python3 scripts/make-icons.py"
  },
  "devDependencies": {
    "linkedom": "^0.18.0"
  }
}
